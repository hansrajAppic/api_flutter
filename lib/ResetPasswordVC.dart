import 'package:flutter/material.dart';
class RestPsword extends StatefulWidget {
  const RestPsword({super.key});

  @override
  State<RestPsword> createState() => _RestPswordState();
}

class _RestPswordState extends State<RestPsword> {
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Column(mainAxisAlignment: MainAxisAlignment.start,
      crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          

      ],)
    );
  }
}